<!DOCTYPE html>
<html>
  <head>
    <title>Blog do Lucas Catom </title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
  </head>

  <body>
    <div class="bg-primary text-white text-center  mb-4 p-4" >
      <%= link_to root_path, class: 'text-white text-decoration-none' do %>
        <h1 class="display-4">Blog do Lucas Caton</h1>
      <% end %>
       <p class="lead">Olá, aqui você encontrará artigos sobre programação,Tecnologia e carreira.</p>   
    </div>
    <div class="container">
      <% flash.each do |type, message|%>
        <div class="alert alert-<%= type == 'alert' ? 'danger' : 'success'%>  alert-dismissible  fade show" role="alert">
            <%=message%>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>

       <%end%> 

        <div class="row">
          <div class="col-4 offset-8">
            <div class="search">
              <%= form_with url: search_posts_path, method: :get, local: true, class: 'form-inline' do |form| %>
               <div class= "d-flex flex-row-reverse mb-3">
                 <%= form.search_field :q, class: 'form-control', required: true, 
                 placeholder: "Buscar" %>
                </div>
                <div class= "d-flex flex-row-reverse mb-3">
                  <%= form.submit 'Busca', class:'btn btn-primary mb-3',
                  data: {disable_with: 'Buscando...'}%>
                  <%end%>
                </div>
            </div>

          </div>

        </div>

      <%= yield %>

    </div>
    <footer class='bg-primary text-white text-center mt-5 p-3'>
      Copyright &copy; Lucas Caton <%= Date.current.year%>.
    </footer>
  </body>
</html>


